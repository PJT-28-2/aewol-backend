<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aewol.domain.wallet.mapper.TossChargeOrderMapper">

    <insert id="insert">
        INSERT INTO toss_charge_order (order_id, member_id, amount, status, created_at)
        VALUES (#{orderId}, #{memberId}, #{amount}, #{status}, NOW())
    </insert>

    <!-- resultType="map"은 camelCase 변환 없이 DB 컬럼명 그대로 반환한다(order_id, member_id 등). -->
    <select id="findByOrderId" resultType="map">
        SELECT order_id, member_id, amount, status, created_at
        FROM toss_charge_order
        WHERE order_id = #{orderId}
    </select>

    <update id="updateStatus">
        UPDATE toss_charge_order
        SET status = #{status}
        WHERE order_id = #{orderId}
    </update>

</mapper>
