<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aewol.domain.wallet.mapper.WalletMapper">

    <select id="findByMemberId" resultType="map">
        SELECT * FROM wallet WHERE member_id = #{memberId}
    </select>

    <select id="findById" resultType="map">
        SELECT * FROM wallet WHERE wallet_id = #{walletId}
    </select>

    <insert id="insert" parameterType="map">
        INSERT INTO wallet (wallet_id, member_id, total_balance)
        VALUES (#{walletId}, #{memberId}, #{totalBalance})
    </insert>

    <update id="updateBalance">
        UPDATE wallet SET total_balance = #{totalBalance}, updated_at = NOW()
        WHERE wallet_id = #{walletId}
    </update>

    <select id="findBucketsByWalletId" resultType="map">
        SELECT * FROM bucket WHERE wallet_id = #{walletId} ORDER BY created_at
    </select>

    <select id="findBucketById" resultType="map">
        SELECT * FROM bucket WHERE bucket_id = #{bucketId}
    </select>

    <insert id="insertBucket" parameterType="map">
        INSERT INTO bucket (bucket_id, wallet_id, pet_id, bucket_type, bucket_name, target_amount, balance, is_sos)
        VALUES (#{bucketId}, #{walletId}, #{petId}, #{bucketType}, #{bucketName}, #{targetAmount}, #{balance}, #{isSos})
    </insert>

    <update id="updateBucket" parameterType="map">
        UPDATE bucket
        SET bucket_name = #{bucketName}, target_amount = #{targetAmount},
            balance = #{balance}, updated_at = NOW()
        WHERE bucket_id = #{bucketId}
    </update>

    <delete id="deleteBucket">
        DELETE FROM bucket WHERE bucket_id = #{bucketId}
    </delete>

</mapper>
