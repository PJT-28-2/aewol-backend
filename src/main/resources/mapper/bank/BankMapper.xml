<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aewol.domain.bank.mapper.BankMapper">

    <!-- 2열 그리드 1행에 KB(004)-토스(092)가 오도록 정렬 (왼쪽 KB, 오른쪽 토스) -->
    <select id="findAll" resultType="map">
        SELECT bank_code, bank_name FROM bank_master
        ORDER BY CASE WHEN bank_code = '004' THEN 0 WHEN bank_code = '092' THEN 1 ELSE 2 END, bank_code
    </select>

</mapper>
