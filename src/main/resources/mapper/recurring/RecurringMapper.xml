<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aewol.domain.recurring.mapper.RecurringMapper">

    <select id="findByWalletId" resultType="map">
        SELECT * FROM recurring_payment WHERE wallet_id = #{walletId} AND is_active = 1
    </select>

    <insert id="insert" parameterType="map">
        INSERT INTO recurring_payment (recurring_id, wallet_id, bucket_id, product_name, amount, cycle_days, next_payment_date)
        VALUES (#{recurringId}, #{walletId}, #{bucketId}, #{productName}, #{amount}, #{cycleDays}, #{nextPaymentDate})
    </insert>

    <update id="deactivate">
        UPDATE recurring_payment SET is_active = 0 WHERE recurring_id = #{recurringId}
    </update>

    <select id="findDuePayments" resultType="map">
        SELECT * FROM recurring_payment WHERE is_active = 1 AND next_payment_date = #{date}
    </select>

</mapper>
