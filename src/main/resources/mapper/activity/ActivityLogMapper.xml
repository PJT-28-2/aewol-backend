<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aewol.domain.activity.mapper.ActivityLogMapper">

    <insert id="insert" parameterType="map">
        INSERT INTO activity_log (log_id, wallet_id, member_id, action_type, target_type, target_id, description)
        VALUES (#{logId}, #{walletId}, #{memberId}, #{actionType}, #{targetType}, #{targetId}, #{description})
    </insert>

    <select id="findByWalletId" resultType="map">
        SELECT * FROM activity_log WHERE wallet_id = #{walletId} ORDER BY created_at DESC
    </select>

    <select id="findByMemberId" resultType="map">
        SELECT * FROM activity_log WHERE member_id = #{memberId} ORDER BY created_at DESC
    </select>

</mapper>
