<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aewol.domain.account.mapper.AccountMapper">
    <!-- linked_account에 bank_name 컴럼 없음(bank_master 조인). account_number_hash는 V4에서 NOT NULL -->

    <insert id="insert" parameterType="map" useGeneratedKeys="true" keyProperty="accountId" keyColumn="account_id">
        INSERT INTO linked_account
            (member_id, bank_code, account_number, account_number_hash, is_primary)
        VALUES
            (#{memberId}, #{bankCode}, #{accountNumber}, #{accountNumberHash}, #{isPrimary})
    </insert>

    <select id="findByMemberId" resultType="map">
        SELECT la.*, bm.bank_name
        FROM linked_account la
        JOIN bank_master bm ON bm.bank_code = la.bank_code
        WHERE la.member_id = #{memberId} AND la.status = 'ACTIVE'
    </select>

    <select id="findByAccountId" resultType="map">
        SELECT la.*, bm.bank_name
        FROM linked_account la
        JOIN bank_master bm ON bm.bank_code = la.bank_code
        WHERE la.account_id = #{accountId}
    </select>

    <!-- disconnectAccount 전용 잠금 조회. 연동 해제 대상 계좌와, 대표 승격 후보 계좌
         모두 이 쿼리로 행을 잠근 뒤 처리해서 동시성 문제를 막는다(CodeRabbit 지적,
         2026-08-08). -->
    <select id="findByAccountIdForUpdate" resultType="map">
        SELECT la.*, bm.bank_name
        FROM linked_account la
        JOIN bank_master bm ON bm.bank_code = la.bank_code
        WHERE la.account_id = #{accountId}
        FOR UPDATE
    </select>

    <!-- INACTIVE로 바뀌는 계좌가 대표 계좌였으면 is_primary도 같이 내린다. 안 내리면
         연동 해제 후에도 DB엔 대표 표시가 남아있어서, 나중에 대표 계좌 설정 로직이
         "이미 대표가 있다"고 잘못 판단할 여지가 생긴다(2026-08-07 코드리뷰 지적). -->
    <update id="updateStatus">
        UPDATE linked_account
        SET status = #{status},
            is_primary = CASE WHEN #{status} = 'INACTIVE' THEN 0 ELSE is_primary END,
            updated_at = NOW()
        WHERE account_id = #{accountId}
    </update>

    <!-- 대표 계좌는 회원당 항상 최대 1개 — 새 대표 지정 전에 기존 대표를 먼저 내린다.
         status 조건 없이 회원의 모든 행(INACTIVE 포함)을 내린다 — ACTIVE로만 좁히면
         과거에 연동 해제된 계좌에 남아있는 is_primary=1이 안 지워진다(CodeRabbit
         지적, 2026-08-07). 배포 전 기존 데이터는 V13 마이그레이션으로 별도 보정한다
         (원래 V9로 작성했으나 번호 충돌로 V13으로 재번호, 2026-08-08). -->
    <update id="clearPrimaryByMemberId">
        UPDATE linked_account SET is_primary = 0, updated_at = NOW()
        WHERE member_id = #{memberId}
    </update>

    <!-- status='ACTIVE' 조건을 걸어서, findByAccountId로 확인한 뒤 setPrimary 실행 전
         사이에 다른 트랜잭션이 이 계좌를 연동 해제(INACTIVE)해도 대표로 올라가지
         않는다. 영향 행 수(0/1)를 그대로 반환해서 서비스가 경쟁 상태를 감지한다
         (CodeRabbit 지적, 2026-08-07). -->
    <update id="setPrimary">
        UPDATE linked_account SET is_primary = 1, updated_at = NOW()
        WHERE account_id = #{accountId} AND status = 'ACTIVE'
    </update>

</mapper>
