<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aewol.domain.pet.mapper.PetMapper">
    <!-- V1 pet: icon_type/profile_img 컴럼 없음. medical_history는 자유 텍스트(V4) -->

    <select id="findById" resultType="map">
        SELECT * FROM pet WHERE pet_id = #{petId} AND is_active = 1
    </select>

    <select id="findByIdAndMemberId" resultType="map">
        SELECT * FROM pet
        WHERE pet_id = #{petId} AND member_id = #{memberId} AND is_active = 1
    </select>

    <select id="findByMemberId" resultType="map">
        SELECT * FROM pet WHERE member_id = #{memberId} AND is_active = 1 ORDER BY created_at
    </select>

    <!-- pet_id는 BIGINT AUTO_INCREMENT -->
    <insert id="insert" parameterType="map" useGeneratedKeys="true" keyProperty="petId" keyColumn="pet_id">
        INSERT INTO pet (member_id, name, species, breed, birth_date, gender,
                        weight, neutered, medical_history)
        VALUES (#{memberId}, #{name}, #{species}, #{breed}, #{birthDate}, #{gender},
                #{weight}, #{neutered}, #{medicalHistory})
    </insert>

    <!-- 소유자 본인의 반려동물만 갱신되도록 member_id를 함께 건다 -->
    <update id="updateCharacterImages">
        UPDATE pet
           SET profile_img = #{profileImg},
               character_img = #{characterImg},
               updated_at = NOW()
        WHERE pet_id = #{petId}
          AND member_id = #{memberId}
          AND is_active = 1
    </update>

    <update id="update" parameterType="map">
        UPDATE pet
        SET name = #{name}, species = #{species}, breed = #{breed},
            birth_date = #{birthDate}, gender = #{gender}, weight = #{weight},
            neutered = #{neutered}, medical_history = #{medicalHistory},
            updated_at = NOW()
        WHERE pet_id = #{petId}
    </update>

    <update id="deactivate">
        UPDATE pet
        SET is_active = 0, updated_at = NOW()
        WHERE pet_id = #{petId} AND member_id = #{memberId} AND is_active = 1
    </update>

    <update id="updateRegistrationNumber">
        UPDATE pet
        SET reg_number = #{regNumber}, updated_at = NOW()
        WHERE pet_id = #{petId} AND member_id = #{memberId} AND is_active = 1
    </update>

    <update id="updateRegistrationDetails">
        UPDATE pet
        SET breed = COALESCE(#{breed}, breed), neutered = COALESCE(#{neutered}, neutered), updated_at = NOW()
        WHERE pet_id = #{petId}
    </update>

</mapper>
