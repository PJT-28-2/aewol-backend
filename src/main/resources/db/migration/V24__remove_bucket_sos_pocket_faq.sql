-- 버킷 개수 제한 / SOS 포켓 FAQ는 대응하는 기능이 없어 삭제한다(2026-08-11, 민주 확인).
-- V16에서 이미 시드된 뒤라 V16 자체를 수정하지 않고(이미 적용된 마이그레이션은
-- Flyway 체크섬 검증 때문에 수정하면 안 됨) 별도 DELETE 마이그레이션으로 정리한다.
-- question 텍스트 기준으로 지우므로 이미 지워졌거나 애초에 없던 환경에서도 안전하다.
DELETE FROM `faq`
WHERE `category` = '지갑·버킷'
  AND `question` IN (
    '버킷은 몇 개까지 만들 수 있나요?',
    'SOS 포켓은 무엇인가요?'
  );
