-- =====================================================================
-- V8: connected_id 컬럼 제거 (2026-08-06)
--
-- CODEF Connected ID 기반 실시간 잔액조회 기능을 폐기하기로 결정했다 —
-- 사용자의 인터넷뱅킹 아이디/비밀번호를 제3자(CODEF)에 넘겨야 하는 신뢰
-- 장벽이 크고, 애월의 핵심 기능(버킷/지출관리)은 이 외부 계좌 잔액이 아니라
-- 내부 지갑(wallet) 잔액만 사용한다. 연동 계좌는 앞으로 본인 확인(1원인증)
-- 용도로만 쓰고, 그 계좌 자체의 잔액은 화면에 표시하지 않는다.
-- =====================================================================

ALTER TABLE `linked_account` DROP COLUMN `connected_id`;
