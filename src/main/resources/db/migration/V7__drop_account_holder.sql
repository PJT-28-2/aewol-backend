-- =====================================================================
-- V7: 예금주명(account_holder) 컬럼 제거 (2026-08-06)
--
-- 1원 인증 흐름에서 사용자가 입력한 예금주명을 CODEF와 실제로 대조 검증하지
-- 않고 DB에 저장만 하고 있었다 — 검증 효과가 없는 값이었고, 프론트 어디에도
-- 표시되지 않아 제거하기로 결정했다(대체 검증이 필요하면 CODEF 계좌성명조회/
-- 계좌실명조회 API를 별도로 붙여야 한다 - 지금은 미사용).
-- =====================================================================

ALTER TABLE `account_verification` DROP COLUMN `account_holder`;
ALTER TABLE `linked_account` DROP COLUMN `account_holder`;
