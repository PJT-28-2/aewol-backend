spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aewol?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: aewol
    password: aewol1234

  data:
    redis:
      host: localhost
      port: 6379

  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false

jwt:
  secret: aewol-test-secret-key-for-jwt-token-generation-minimum-256-bits-required
  access-token-expiry: 1800000
  refresh-token-expiry: 604800000

logging:
  level:
    com.aewol: DEBUG
